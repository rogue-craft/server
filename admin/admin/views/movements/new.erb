<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:movements, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:movements, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :movement, url(:movements, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'movements/form', :locals => { :f => f } %>
 <% end %>
</div>
