<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:movements, :index) %></li>
  <li> <%= link_to tag_icon(:plus, pat(:new)), url(:movements, :new) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, pat(:edit)), url(:movements, :edit, :id => @movement.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :movement, url(:movements, :update, :id => @movement.id), :method => :put, :class => 'form-horizontal' do |f| %>
    <%= partial 'movements/form', :locals => { :f => f } %>
  <% end %>
</div>
