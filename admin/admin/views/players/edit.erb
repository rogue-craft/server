<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:players, :index) %></li>
  <li> <%= link_to tag_icon(:plus, pat(:new)), url(:players, :new) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, pat(:edit)), url(:players, :edit, :id => @player.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :player, url(:players, :update, :id => @player.id), :method => :put, :class => 'form-horizontal' do |f| %>
    <%= partial 'players/form', :locals => { :f => f } %>
  <% end %>
</div>
